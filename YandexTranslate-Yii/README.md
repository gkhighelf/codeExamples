codeExamples
============

code examples

База для создания любого API и реализация переводчика яндекса на нём.

Translator usage

$tr = new YandexTranslator( API_KEY );
$russian_text = $tr->translate( $ukraine_text );